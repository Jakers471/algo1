# engine/ — confirmed, live-ready strategy pieces

This folder holds only **solidified** components: hard, fast, clean, no research or
plotting, built to drop into a backtest or a live API loop with nothing in the way.

**Promotion rule (hard):** a piece enters `engine/` ONLY after it is built and proven
in `research/` **and the user explicitly says to move it.** Claude never promotes on
its own — **the user decides when.** research = discover; engine = execute.

Every promotion is logged in `../CHECKLIST.md` (Promotion log).

_Empty until the first piece is confirmed._
