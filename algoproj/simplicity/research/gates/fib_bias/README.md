# fib_bias

**Purpose:** Fib off session hi/lo → directional lean (bias gate). Chart overlay built; the bias/edge test is UNTESTED (run with a baseline, like session_break_stats).
**Role:** gate/component in the LIVE state machine — runs on the session's bars-so-far, feeds the arm/disarm confluence (see `../../../ARCHITECTURE.md` "Runtime model").
**Attached to:** VISION 10 · CHECKLIST Phase 5-6 (gate)
**Status:** [~] chart overlay built · edge test TBD
